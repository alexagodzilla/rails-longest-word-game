<div class="container">
  <h1 class="mb-5">New Game</h1>
  <div class="row d-flex justify-content-around">
    <div class="col-1"></div>
    <% @letters.each do |letter| %>
      <div class="col-1 cell text-center p-0"><span class="letters"><%= letter.upcase %></span></div>
    <% end %>
    <div class="col-1"></div>
  </div>
    <div class="mt-5">
      <p>What is the longest word you can find?</p>
       <!-- <form action="/score" method="POST" data-turbo="false">
          <input id="member" name="guess" type="text" value="<%= params[:guess] %>" placeholder="Your Guess">
          <input type="submit" value="Play">
          <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
        </form>
        -->
        <form class="input-group input-group-md" action="/score" method="POST" data-turbo="false">
          <input id="question" type="text" name="question" value="<%= params[:question] %>" class="form-control" aria-label="Sizing example input" aria-describedby="inputGroup-sizing-md">
          <input type="submit" class="btn btn-primary" value="Play">
          <%= hidden_field_tag :letters, @letters %>
          <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
        </form>
    </div>
</div>
